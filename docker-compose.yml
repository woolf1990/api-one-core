version: '3.8'
services:
  app:
    build: .
    ports:
      - '8000:8000'
    volumes:
      - ./:/app
  # Optional SQL Server service for integration (uncomment and configure if you want)
  # sqlserver:
  #   image: mcr.microsoft.com/mssql/server:2019-latest
  #   environment:
  #     - SA_PASSWORD=Your_password123
  #     - ACCEPT_EULA=Y
  #   ports:
  #     - '1433:1433'
